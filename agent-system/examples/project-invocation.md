# Project invocation examples — ApplyVault

## Start orchestration

```text
/operate
```

Then choose **A** for an implementation plan only (recommended when no plan exists yet).

## Concrete request

```text
/operate
Improve CV builder inline editing consistency with the section schema catalog.
```

Architect should select frontend-engineer (+ backend/ai if catalog/API touched), write delegation envelopes, and use project skills for tickets/implementation when coding is authorized.

## Brownfield constraint reminder

Do not replace Supabase or introduce payments without a new approval gate.
